# 更新日志

本项目采用语义化版本号：`主版本.次版本.修订版本`。

## v1.1.0 - 2026-08-20

### 主要内容

- 优化发布、城市、账号、直播和素材相关页面的顶部说明区域，减少标题说明占用空间，将主要操作收纳为更紧凑的按钮区域。
- 优化素材录入页面布局，提升文件夹树、素材列表、上传统计和内容区域的可用空间。
- 素材录入文件夹树支持右键菜单，可在空白区域创建文件树顶级文件夹，也可创建同级或子级文件夹。
- 修复创建文件树顶级文件夹时误展开“我的素材”的问题。
- 系统设置页面调整为正式版配置布局，设置项横向展示，配置内容按页面分区展示。
- 存储桶设置支持在腾讯云 COS 和又拍云之间切换，并新增又拍云连接测试与素材上传/删除适配。

### 部署提醒

- 前端部署请重新执行 `npm run build`，并覆盖线上静态资源目录。
- 后端部署需要覆盖本次更新涉及的接口与存储服务文件，并重启后端服务。
- 不要覆盖线上 `.env`、SQLite 数据库、上传目录和日志目录，避免影响生产数据。

## v1.0.0 - 2026-06-20

初次可部署版本。

### 主要内容

- 管理端工作台、素材、发布、城市、账号、数据、AI 日报等核心页面。
- 城市端工作台、任务、发布填报、数据录入、通知、账号页面。
- 页面布局中心：支持页面组件拖拽、显示隐藏、宽高调整、字段绑定和后端持久化。
- 系统设置中心：支持品牌、存储桶、页面文案、展示字段、偏好配置。
- 登录认证、城市账号权限隔离、通知、操作日志和基础测试。
- 登录安全：默认 12 小时登录有效期、滑块验证、登录失败限流。

### 部署提醒

- 生产环境必须配置 `JWT_SECRET`、`ADMIN_PASSWORD`、`DB_PATH` 和 COS 相关参数。
- 不要直接复制本机 `node_modules` 到服务器，服务器上需要重新安装依赖。
